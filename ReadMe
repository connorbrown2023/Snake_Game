#Snake Game

Description
This is a recreation of the classic Snake game using Python's Turtle graphics module. The objective is to control a snake to eat food, which makes the snake grow longer. The game ends when the snake collides with itself or the wall.

## Installation
1. Clone the repository:
    ```sh
    git clone https://github.com/connorbrown2023/Snake_Game.git
    ```
2. Navigate to the project directory:
    ```sh
    cd snake-game
    ```
3. Install the required dependencies:
    ```sh
    pip install -r requirements.txt
    ```

## Usage
To start the game, run the main script:
```sh
python main.py

##Controls
Use the arrow keys to control the snake:

Up: Move up
Down: Move down
Left: Move left
Right: Move right

##Improvements

Future improvements could include:

-Keeping track of the highest score.
-Adding levels with increasing difficulty.
-Introducing obstacles that the snake must avoid.
-Adding sound effects and background music.